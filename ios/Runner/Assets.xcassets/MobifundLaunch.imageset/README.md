Placeholders: run `decode_ios_launch_assets.ps1` from the repo root to decode base64 files into PNGs.

The script will produce:
- mobifund_launch.png
- mobifund_launch@2x.png
- mobifund_launch@3x.png

Replace them with higher-resolution PNGs exported from the SVG if you want better fidelity.
